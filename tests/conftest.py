"""Shared test fixtures.

Test modules are added by the milestone that owns the behavior they prove.
This file exists so that pytest has a rootdir anchor and so that the package
under test is importable from the source tree.
"""
